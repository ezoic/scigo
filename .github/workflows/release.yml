name: Release

on:
  push:
    tags:
      - 'v*.*.*'
  workflow_dispatch:
    inputs:
      tag:
        description: 'Tag to release (e.g., v0.2.0)'
        required: true
        type: string

permissions:
  contents: write
  packages: write
  
jobs:
  release:
    name: Create Release
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
        
    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version: '1.24'
        
    - name: Run tests
      run: |
        go test -v -race -coverprofile=coverage.txt -covermode=atomic ./...
        
    - name: Check test coverage
      run: |
        go tool cover -func=coverage.txt
        total=$(go tool cover -func=coverage.txt | grep total | awk '{print $3}' | sed 's/%//')
        if (( $(echo "$total < 70" | bc -l) )); then
          echo "Test coverage is below 70% ($total%)"
          exit 1
        fi
        echo "Test coverage: $total%"
        
    - name: Build binaries
      run: |
        # Build for multiple platforms
        GOOS=linux GOARCH=amd64 go build -o scigo-linux-amd64 ./cmd/scigo
        GOOS=linux GOARCH=arm64 go build -o scigo-linux-arm64 ./cmd/scigo
        GOOS=darwin GOARCH=amd64 go build -o scigo-darwin-amd64 ./cmd/scigo
        GOOS=darwin GOARCH=arm64 go build -o scigo-darwin-arm64 ./cmd/scigo
        GOOS=windows GOARCH=amd64 go build -o scigo-windows-amd64.exe ./cmd/scigo
        
    - name: Generate release notes
      id: changelog
      env:
        EVENT_NAME: ${{ github.event_name }}
        INPUT_TAG: ${{ github.event.inputs.tag }}
        REF_NAME: ${{ github.ref_name }}
      run: |
        # Extract version from tag or input
        VERSION=${GITHUB_REF#refs/tags/}
        if [ "$EVENT_NAME" == "workflow_dispatch" ]; then
          VERSION="$INPUT_TAG"
        fi
        echo "VERSION=$VERSION" >> $GITHUB_OUTPUT
        
        # Extract changelog section for this version
        if grep -q "## \[$VERSION\]" CHANGELOG.md; then
          sed -n "/## \[$VERSION\]/,/## \[/p" CHANGELOG.md | sed '$d' > release_notes.md
        else
          sed -n "/## \[Unreleased\]/,/## \[/p" CHANGELOG.md | sed '$d' > release_notes.md
        fi
        
        # Add installation instructions
        cat >> release_notes.md << 'EOF'
        
        ## Installation
        
        ### Go Install (Recommended)
        ```bash
        go install github.com/ezoic/scigo@latest
        ```
        
        ### Go Module
        ```bash
        go get github.com/ezoic/scigo@VERSION
        ```
        
        ## Documentation
        
        - [API Documentation](https://pkg.go.dev/github.com/ezoic/scigo)
        - [Examples](https://github.com/ezoic/scigo/tree/main/examples)
        - [Migration Guide](https://github.com/ezoic/scigo#-scikit-learn-compatibility)
        EOF
        
        # Replace VERSION placeholder
        sed -i "s/@VERSION/@$VERSION/g" release_notes.md
        
    - name: Create Release
      uses: softprops/action-gh-release@v2
      with:
        tag_name: ${{ steps.changelog.outputs.VERSION }}
        name: Release ${{ steps.changelog.outputs.VERSION }}
        body_path: release_notes.md
        draft: false
        prerelease: ${{ contains(steps.changelog.outputs.VERSION, '-alpha') || contains(steps.changelog.outputs.VERSION, '-beta') || contains(steps.changelog.outputs.VERSION, '-rc') }}
        files: |
          scigo-linux-amd64
          scigo-linux-arm64
          scigo-darwin-amd64
          scigo-darwin-arm64
          scigo-windows-amd64.exe
        generate_release_notes: true
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          
    - name: Update pkg.go.dev
      run: |
        # Trigger pkg.go.dev to update
        curl "https://proxy.golang.org/github.com/ezoic/scigo/@v/${{ steps.changelog.outputs.VERSION }}.info"
        
    - name: Notify release
      uses: actions/github-script@v7
      with:
        script: |
          const version = '${{ steps.changelog.outputs.VERSION }}';
          const release_url = `https://github.com/${context.repo.owner}/${context.repo.repo}/releases/tag/${version}`;
          
          // Create an issue to track post-release tasks
          await github.rest.issues.create({
            owner: context.repo.owner,
            repo: context.repo.repo,
            title: `Post-release tasks for ${version}`,
            body: `## Post-release checklist for ${version}\n\n` +
                  `- [ ] Verify pkg.go.dev updated: https://pkg.go.dev/github.com/ezoic/scigo\n` +
                  `- [ ] Update documentation if needed\n` +
                  `- [ ] Announce release on social media\n` +
                  `- [ ] Update dependent projects\n` +
                  `- [ ] Check for any reported issues\n\n` +
                  `Release URL: ${release_url}`,
            labels: ['release', 'documentation']
          });